#pragma once
#include <iostream>
#include <stdio.h>      
#include <stdarg.h> 
using namespace std;
int SumMultiples(int r) {
	int total = 0;
	for (int i = 1; i < r; i++) {
		if (i%3 == 0 || i%5 ==0) {
			
				total = i + total;
			}
			
			
		}
	
	return total;  
}
