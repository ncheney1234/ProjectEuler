#include <iostream>
#include <list>
#include <cmath>
#include <iomanip>
#include <algorithm>
#include <fstream>
#include <string>
#include <vector>
#include <sstream>
#include <deque>
#include <ctime>
#include <cstring>
#include <cstdlib>
#include <map>

using namespace std;

int main() {
	unsigned t0 = clock();
	long long myints[] = { 1, 2, 3, 4, 5, 6, 7, 8, 9 };


	do {
		if ((myints[0] * 100 + myints[1] * 10 + myints[2]) * 2 == (myints[3] * 100 + myints[4] * 10 + myints[5]) && (myints[0] * 100 + myints[1] * 10 + myints[2]) * 3 == (myints[6] * 100 + myints[7] * 10 + myints[8])) {
			cout << myints[0] << myints[1] << myints[2] << myints[3] << myints[4] << myints[5] << myints[6] << myints[7] << myints[8] << endl;
		}
		if ((myints[0] * 1000 + myints[1] * 100 + myints[2] * 10 + myints[3]) * 2 == myints[4] * 10000 + myints[5] * 1000 + myints[6] * 100 + myints[7] * 10 + myints[8]) {
			cout << myints[0] << myints[1] << myints[2] << myints[3] << myints[4] << myints[5] << myints[6] << myints[7] << myints[8] << endl;
		}
		if ((myints[0] * 10 + myints[1]) * 2 == myints[2] * 10 + myints[3] && (myints[0] * 10 + myints[1]) * 3 == (myints[4] * 10 + myints[5]) && (myints[0] * 10 + myints[1]) * 4 == (myints[6] * 100 + myints[7] * 10 + myints[8])) {
			cout << myints[0] << myints[1] << myints[2] << myints[3] << myints[4] << myints[5] << myints[6] << myints[7] << myints[8] << endl;
		}
	} while (next_permutation(myints, myints + 9));



std::cout << endl;
unsigned elapsed = clock() - t0;
std::cout << "Elapsed time: " << elapsed << endl;
std::system("pause");
return 0;

}
